[app]
name = "yuliitezary-bot"
primary_region = "ams"

[build]
  dockerfile = "Dockerfile"

[env]
  # BOT_TOKEN задаётся через: fly secrets set BOT_TOKEN=...

[[vm]]
  memory = "256mb"
  cpu_kind = "shared"
  cpus = 1
